% ABM vs ODE/PDE Comparison — Compact Side-by-Side
\begin{tikzpicture}[
    colbox/.style={draw, rounded corners=3pt, text width=4.8cm,
        align=center, font=\small, inner sep=5pt, minimum height=1.1cm},
    mathbox/.style={draw, rounded corners=3pt, fill=gray!10,
        text width=4.8cm, align=center, font=\small, inner sep=5pt},
    hybridbox/.style={draw, rounded corners=4pt, fill=immunecell!8,
        text width=11.2cm, align=center, font=\small, inner sep=6pt},
    coltitle/.style={font=\small\bfseries}
]

% ── Vertical divider ────────────────────────────────────────
\draw[thick, densely dashed, gray!50] (6.1,-0.6) -- (6.1,9.6);

% ── Column titles ───────────────────────────────────────────
\node[coltitle, immunecell] at (3,9.2) {Agent-Based Model (ABM)};
\node[coltitle, dna]        at (9.2,9.2) {Equation-Based (ODE/PDE)};

% ── ABM boxes (left) ────────────────────────────────────────
\node[colbox, fill=tumorcell!12] (a1) at (3,8)
    {\textbf{Individual Agents}\\[1pt]
     \footnotesize Each cell has unique state};

\node[colbox, fill=bloodvessel!12] (a2) at (3,6.4)
    {\textbf{3D Spatial Grid}\\[1pt]
     \footnotesize Local neighbour interactions};

\node[colbox, fill=immunecell!12] (a3) at (3,4.8)
    {\textbf{Stochastic Rules}\\[1pt]
     \footnotesize Probabilistic decisions};

\node[colbox, fill=treatment!12] (a4) at (3,3.2)
    {\textbf{Emergent Behavior}\\[1pt]
     \footnotesize Patterns from local rules};

\node[mathbox] (arule) at (3,1.6)
    {\footnotesize\textit{Typical rule}\\[2pt]
     $P_{\mathrm{kill}} = f(\text{activation},\;\text{glucose},\;\text{effects})$};

% ── ODE boxes (right) ───────────────────────────────────────
\node[colbox, fill=dna!12] (o1) at (9.2,8)
    {\textbf{Population Variables}\\[1pt]
     \footnotesize $T(t),\; I(t),\; C(\mathbf{x},t)$};

\node[colbox, fill=bloodvessel!12] (o2) at (9.2,6.4)
    {\textbf{Continuous Fields}\\[1pt]
     \footnotesize Smooth spatial averages};

\node[colbox, fill=glucose!20] (o3) at (9.2,4.8)
    {\textbf{Deterministic Dynamics}\\[1pt]
     \footnotesize Fixed trajectories};

\node[colbox, fill=gray!12] (o4) at (9.2,3.2)
    {\textbf{Mean-Field Approximation}\\[1pt]
     \footnotesize Well-mixed assumption};

\node[mathbox] (orule) at (9.2,1.6)
    {\footnotesize\textit{Typical equation}\\[2pt]
     $\dfrac{dT}{dt} = rT\!\left(1-\tfrac{T}{K}\right) - kTI$};

% ── Connecting arrows within columns ────────────────────────
\foreach \i/\j in {a1/a2, a2/a3, a3/a4}{
    \draw[->, >=stealth, gray!60] (\i) -- (\j);}
\foreach \i/\j in {o1/o2, o2/o3, o3/o4}{
    \draw[->, >=stealth, gray!60] (\i) -- (\j);}

% ── Hybrid box (bottom) ─────────────────────────────────────
\node[hybridbox] (hybrid) at (6.1,0)
    {\textbf{Hybrid (this work):} ABM cells + PDE glucose field};

\draw[->, >=stealth, thick, immunecell!60] (arule.south) -- ++(0,-0.45) -| (hybrid.north -| 3,0);
\draw[->, >=stealth, thick, dna!60]        (orule.south) -- ++(0,-0.45) -| (hybrid.north -| 9.2,0);

\end{tikzpicture}
